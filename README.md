# DTA-Multiagent-Pipeline
Drug-target affinity (DTA) models perform well on curated benchmarks but fail under real-world noise. This project builds a multi-agent pipeline that quantifies prediction uncertainty, detects distribution shift, and ranks compounds by expected utility under a fixed wet-lab budget.
